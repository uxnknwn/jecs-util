base: roblox
name: selene_defs
globals:
    # override Roblox require style with string requires
    require:
        args:
            - type: string
    describe:
        args:
            - type: string
            - type: function
    it:
        args:
            - type: string
            - type: function
